# كيفية بناء واختبار ميزة معلومات الشبكة

## الخلاصة السريعة
تم إضافة ميزة جديدة تعرض معلومات الشبكة (عنوان IP، URL، حالة الاتصال) في صفحة الإعدادات. هذه الميزة مهمة للعمل على عدة أجهزة متصلة بنفس الشبكة.

### الإصلاحات التي تم تطبيقها:
1. ✅ إضافة `[AllowAnonymous]` على endpoint `/api/system/info` 
2. ✅ إضافة `[AllowAnonymous]` على endpoint `/api/system/health`
3. ✅ إضافة RTK Query hooks: `useGetSystemInfoQuery()` و `useHealthQuery()`
4. ✅ إضافة Network Info Card في صفحة Settings
5. ✅ إضافة polling interval (5 seconds) لـ health check

---

## خطوات البناء والاختبار

### الخطوة 1: فتح Terminal/PowerShell

اضغط `Windows + X` ثم اختر **Windows PowerShell (Admin)**

### الخطوة 2: الانتقال لمجلد البناء

```powershell
cd 'd:\مسح\POS\Deployment\Scripts'
```

### الخطوة 3: تشغيل البناء

```powershell
.\BUILD_ALL.ps1
```

هذا سيقوم بـ:
- ✅ بناء Backend (.NET APIs)
- ✅ بناء Frontend (React)
- ✅ نسخ الملفات لـ `output/kasserpro-allinone`
- ⏱️ قد يستغرق 3-5 دقائق

### الخطوة 4: انتظار انتهاء البناء

سترى رسالة مثل:
```
✓ BUILD COMPLETE
✓ Output ready at: d:\مسح\POS\output\kasserpro-allinone
```

---

## اختبار الميزة

### على الجهاز الرئيسي (حيث الـ API يعمل):

#### 1. تشغيل التطبيق
```powershell
cd 'd:\مسح\POS\output\kasserpro-allinone'
.\START.bat
```

#### 2. فتح الموقع في المتصفح
سيفتح تلقائياً عند `http://localhost:5243`

#### 3. تسجيل الدخول
استخدم بيانات الدخول العادية

#### 4. الذهاب لصفحة الإعدادات
- انقر على **⚙️ اسم الشركة** في الزاوية العلوية اليمين
- اختر **🔧 الإعدادات**

#### 5. البحث عن كارت معلومات الشبكة
يجب أن ترى:

```
🔗 معلومات الشبكة           [متصل] (أخضر)

عنوان للأجهزة الأخرى:
http://192.168.X.X:5243      [📋 نسخ]

عنوان IP        |  المنفذ
192.168.X.X     |  5243

📱 استخدم هذا العنوان على الموبايل، التابلت، أو أي جهاز آخر 
في نفس الشبكة
```

---

## استكشاف الأخطاء

### المشكلة: كارت معلومات الشبكة غير مرئي
**الحل:**
- تأكد من تسجيل الدخول أولاً (البيانات مخفية بدون تسجيل دخول)
- افتح متصفح DevTools بـ F12 وافحص _Console_ لرسائل الخطأ
- إذا رأيت `401 Unauthorized`، تأكد من البناء بـ `BUILD_ALL.ps1`

### المشكلة: الـ URL يظهر `127.0.0.1` بدلاً من IP الفعلي
**السبب:** هذا طبيعي إذا لم تكن متصل بـ network interface
**الحل:** تأكد من أن جهازك متصل بـ WiFi أو LAN

### المشكلة: "غير متصل" يظهر بدل "متصل"
**الحل:**
- تأكد من عدم إيقاف الـ API
- جرّب F5 لإعادة تحميل الصفحة
- افحص أن الـ backend يعمل (اضغط Ctrl+C في terminal الـ backend)

---

## التحقق المتقدم

### 1. فحص Endpoint مباشرة
اضغط في المتصفح:
```
http://localhost:5243/api/system/info
```

يجب أن ترى:
```json
{
  "success": true,
  "data": {
    "lanIp": "192.168.X.X",
    "hostname": "YOUR-PC-NAME",
    "port": 5243,
    "url": "http://192.168.X.X:5243",
    "environment": "Production",
    "timestamp": "2026-02-22T10:30:00Z",
    "isOffline": false
  }
}
```

### 2. فحص Health Endpoint
```
http://localhost:5243/api/system/health
```

يجب أن ترى:
```json
{
  "success": true,
  "status": "healthy",
  "timestamp": "2026-02-22T10:30:00Z"
}
```

---

## استخدام على أجهزة أخرى (Mobile/Tablet/Client PC)

### بعد البناء الناجح:

1. **تحديد IP الجهاز الرئيسي:**
   - اذهب إلى Settings على الجهاز الرئيسي
   - انسخ الـ URL (مثل: `http://192.168.1.105:5243`)

2. **فتح على جهاز آخر:**
   - على جهاز آخر في نفس الـ WiFi
   - انسخ الـ URL في المتصفح
   - يجب أن ترى صفحة تسجيل الدخول نفسها

3. **تسجيل الدخول:**
   - استخدم نفس بيانات الدخول
   - البيانات متزامنة لأنها من Database واحد

---

## الملفات المعدّلة

| الملف | الغرض | الحالة |
|------|-------|--------|
| `backend/KasserPro.API/Controllers/SystemController.cs` | Backend endpoints | ✅ مكتمل |
| `frontend/src/api/systemApi.ts` | RTK Query hooks | ✅ مكتمل |
| `frontend/src/pages/settings/SettingsPage.tsx` | UI component | ✅ مكتمل |
| `backend/KasserPro.API/appsettings.Production.json` | Network config | ✅ مكتمل |

---

## ملاحظات مهمة

⚠️ **هام:**
- بعد أي تعديل على الـ Backend (C# code)، يجب تشغيل `BUILD_ALL.ps1`
- بعد أي تعديل على الـ Frontend (React code)، يجب تشغيل `BUILD_ALL.ps1`
- الـ Network feature تتطلب الـ rebuild الكامل

📌 **نصيحة:**
- احفظ ملف `BUILD_ALL.ps1` في المفضلة لسهولة التشغيل المستقبلي
- يمكنك تشغيل البناء مقدماً وترك الجهاز يعمل بدون مراقبة

✅ **بعد التحديث:**
- تأكد من وجود الـ Network Info Card في Settings
- جرّب نسخ الـ URL واستخدمه على جهاز آخر
- تحقق من أن الـ Status يتغير من "متصل" إلى "غير متصل" عند قطع الاتصال

---

## إذا لزم الأمر - التحقق اليدوي

### فحص ملف Backend Config:
```
d:\مسح\POS\backend\KasserPro.API\appsettings.Production.json
```
يجب أن يحتوي على:
```json
"Urls": "http://0.0.0.0:5243",
"AllowedOrigins": ["*"]
```

### فحص API Hooks في Frontend:
```
d:\مسح\POS\frontend\src\api\systemApi.ts
```
يجب أن يحتوي على:
```typescript
getSystemInfo: builder.query<...>({
  query: () => "/system/info",
}),
health: builder.query<...>({
  query: () => "/system/health",
  pollingInterval: 5000,
}),
```

---

## الدعم والمساعدة

إذا واجهت أي مشاكل:
1. ✅ تأكد من تشغيل `BUILD_ALL.ps1`
2. ✅ جرّب F5 لإعادة تحميل الصفحة
3. ✅ افحص F12 Console للأخطاء
4. ✅ تأكد من تسجيل الدخول
5. ✅ جرّب إعادة تشغيل المتصفح والـ API

---

**آخر تحديث:** 2026-02-22  
**الحالة:** جاهز للاختبار
