# Desktop Bridge App - دليل الاستخدام السريع

## ✅ التطبيق يعمل بنجاح!

التطبيق يعمل الآن في System Tray. ابحث عن الأيقونة في شريط المهام بجانب الساعة.

## خطوات الإعداد

### 1. افتح نافذة الإعدادات
- اضغط **double-click** على أيقونة التطبيق في System Tray
- أو اضغط **right-click** واختر "Settings"

### 2. أدخل البيانات المطلوبة

#### Backend URL
```
https://localhost:5243
```
أو عنوان الـ Backend Server الخاص بك

#### API Key
أدخل أي قيمة للاختبار (مثلاً: `test-api-key`)

في الإصدار النهائي، ستحصل على API Key من لوحة التحكم

#### Default Printer
اختر الطابعة الحرارية من القائمة المنسدلة

إذا لم تظهر الطابعة:
- اضغط "Refresh Printers"
- تأكد من تثبيت الطابعة في Windows

### 3. احفظ الإعدادات
اضغط زر **Save**

سيحاول التطبيق الاتصال بالـ Backend تلقائياً

## اختبار الطباعة

### من System Tray Menu
1. اضغط **right-click** على أيقونة التطبيق
2. اختر **"Test Print"**
3. تحقق من طباعة إيصال تجريبي

### من Backend API
عندما يكون الـ Backend يعمل، سيرسل أوامر الطباعة تلقائياً عند إتمام عمليات البيع

## حالة الاتصال

### الألوان في System Tray
- 🟢 **Connected**: متصل بالـ Backend
- 🔴 **Disconnected**: غير متصل
- 🟡 **Reconnecting**: يحاول إعادة الاتصال

### Toast Notifications
ستظهر إشعارات عند:
- الاتصال بنجاح
- فقدان الاتصال
- أخطاء الطباعة

## عرض السجلات (Logs)

### من System Tray Menu
1. اضغط **right-click** على الأيقونة
2. اختر **"View Logs"**
3. سيفتح ملف السجل في Notepad

### موقع السجلات
```
%AppData%\KasserPro\logs\bridge-app{date}.log
```

مثال:
```
C:\Users\YourName\AppData\Roaming\KasserPro\logs\bridge-app20260131.log
```

## موقع ملف الإعدادات

```
%AppData%\KasserPro\settings.json
```

مثال:
```
C:\Users\YourName\AppData\Roaming\KasserPro\settings.json
```

## تشغيل الـ Backend

### إذا لم يكن الـ Backend يعمل

افتح PowerShell أو CMD في مجلد المشروع:

```bash
cd src/KasserPro.API
dotnet run
```

انتظر حتى ترى:
```
Now listening on: https://localhost:5243
```

## استكشاف الأخطاء

### المشكلة: "API Key not configured"
**الحل**: افتح Settings وأدخل API Key

### المشكلة: "No default printer configured"
**الحل**: افتح Settings واختر طابعة من القائمة

### المشكلة: "Failed to connect to Device Hub"
**الحل**: 
1. تأكد من تشغيل الـ Backend
2. تحقق من Backend URL في Settings
3. تأكد من عدم وجود Firewall يمنع الاتصال

### المشكلة: "Printer not found"
**الحل**:
1. تأكد من تثبيت الطابعة في Windows
2. اضغط "Refresh Printers" في Settings
3. اختر الطابعة الصحيحة

### المشكلة: الطباعة لا تعمل
**الحل**:
1. تحقق من أن الطابعة متصلة وتعمل
2. جرب "Test Print" من القائمة
3. افحص السجلات (View Logs)

## إيقاف التطبيق

### من System Tray Menu
1. اضغط **right-click** على الأيقونة
2. اختر **"Exit"**

سيتم:
- قطع الاتصال بالـ Backend بشكل آمن
- حفظ جميع الإعدادات
- إغلاق التطبيق

## معلومات تقنية

### المنافذ المستخدمة
- Backend API: `5243` (HTTPS)
- SignalR Hub: `/hubs/devices`

### البروتوكولات
- SignalR over WebSocket Secure (WSS)
- Automatic reconnection: 0s, 2s, 5s, 10s

### متطلبات النظام
- Windows 10/11
- .NET 9.0 Runtime
- طابعة حرارية متوافقة مع ESC/POS

## الميزات المتاحة (MVP)

✅ اتصال SignalR مع الـ Backend
✅ إعادة الاتصال التلقائية
✅ استقبال أوامر الطباعة
✅ طباعة إيصالات ESC/POS
✅ إرسال حالة الطباعة للـ Backend
✅ واجهة System Tray
✅ نافذة الإعدادات
✅ طباعة تجريبية
✅ عرض السجلات

## الميزات القادمة

⏳ قارئ الباركود
⏳ التحكم في درج النقود
⏳ قائمة انتظار الأوامر (Offline Mode)
⏳ دعم أجهزة متعددة
⏳ سجل مهام الطباعة

---

## الدعم الفني

إذا واجهت أي مشكلة:
1. افحص السجلات (View Logs)
2. تأكد من تشغيل الـ Backend
3. تحقق من الإعدادات (Settings)
4. جرب إعادة تشغيل التطبيق

**الحالة الحالية**: ✅ التطبيق يعمل بنجاح
**آخر تحديث**: 31 يناير 2026
