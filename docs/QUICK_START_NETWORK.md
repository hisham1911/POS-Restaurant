# Network Feature - Quick Start Guide

## ⚡ 30-Second Summary

تم إضافة ميزة **معلومات الشبكة** في صفحة الإعدادات تعرض:
- عنوان IP للجهاز
- الرابط الكامل للوصول من أجهزة أخرى
- حالة الاتصال (متصل/غير متصل)
- زر نسخ الرابط

---

## 🚀 الخطوات السريعة

### 1️⃣ فتح PowerShell (كمسؤول)
```powershell
Windows + X → Windows PowerShell (Admin)
```

### 2️⃣ الانتقال لمجلد البناء والبدء
```powershell
cd 'd:\مسح\POS\Deployment\Scripts'
.\BUILD_ALL.ps1
```
⏱️ انتظر 3-5 دقائق للانتهاء

### 3️⃣ تشغيل التطبيق
```powershell
cd '..\output\kasserpro-allinone'
.\START.bat
```

### 4️⃣ اختبار الميزة
1. المتصفح سيفتح تلقائياً
2. سجّل الدخول (Admin)
3. اضغط على ⚙️ الإعدادات
4. ابحث عن **"معلومات الشبكة"** 

---

## ✅ ما يجب أن تراه

```
┌─────────────────────────────────┐
│ 🔗 معلومات الشبكة   [متصل] ✅ │
├─────────────────────────────────┤
│ عنوان للأجهزة الأخرى:          │
│ http://192.168.1.100:5243  [📋] │
├─────────────────────────────────┤
│ عنوان IP          المنفذ       │
│ 192.168.1.100     5243         │
├─────────────────────────────────┤
│ 📱 استخدم على موبايل/تابلت   │
│    أو جهاز آخر في الشبكة      │
└─────────────────────────────────┘
```

---

## ❌ إذا لم ترى الميزة

| المشكلة | الحل |
|--------|------|
| الاعدادات لا تظهر | تأكد من Admin login |
| الكارت فارغ | اضغط F5 لإعادة تحميل |
| API Error في Console | أعد تشغيل BUILD_ALL.ps1 |
| "غير متصل" يظهر | جرّب F5 أو أعد تشغيل |

---

## 🧪 طريقة التحقق المتقدمة

في المتصفح:
```
http://localhost:5243/api/system/info
```

يجب أن ترى (إذا الـ JSON):
```json
{
  "success": true,
  "data": {
    "lanIp": "192.168.X.X",
    "hostname": "YOUR-COMPUTER",
    "port": 5243,
    "url": "http://192.168.X.X:5243",
    ...
  }
}
```

---

## 📱 استخدام على جهاز آخر

1. **على الجهاز الرئيسي (Server):** انسخ الـ URL من الكارت
2. **على جهاز ثاني (Tablet/Mobile/PC):**
   - ألصق الـ URL في المتصفح
   - سيفتح نفس التطبيق
   - البيانات متزامنة!

---

## 📋 Checklist الانتهاء

- [ ] `BUILD_ALL.ps1` اكتمل بدون أخطاء
- [ ] `START.bat` يعمل بدون مشاكل
- [ ] حمّلت الصفحة كـ Admin
- [ ] رأيت كارت "معلومات الشبكة"
- [ ] IP address يظهر بشكل صحيح
- [ ] Status يظهر "متصل"
- [ ] زر النسخ يعمل

---

## 💾 الملفات المعدّلة

تم تحديث 4 ملفات فقط:
- ✅ Backend: SystemController.cs (endpoints)
- ✅ Backend: appsettings.Production.json (config)
- ✅ Frontend: systemApi.ts (API hooks)
- ✅ Frontend: SettingsPage.tsx (UI component)

---

## ⚠️ ملاحظات مهمة

1. **يجب البناء بعد كل تعديل** - `BUILD_ALL.ps1`
2. **Multi-device يتطلب نفس الـ Network** (WiFi أو LAN)
3. **البيانات محلية** (SQLite على الجهاز الرئيسي فقط)
4. **Offline mode يعمل تلقائياً** - بدون بناء إضافي

---

## 🆘 للمساعدة

إذا لم تعمل الميزة:
1. افتح DevTools (F12)
2. انظر لـ Network tab للـ API calls
3. انظر لـ Console tab للـ errors
4. تحقق من أن الـ backend يعمل (يجب نافذة Terminal تظهر)

---

**التاريخ:** 2026-02-22  
**الحالة:** ✅ جاهز للاستخدام
