# كيفية استخدام Desktop Bridge App

## ✅ التطبيق تم تحديثه!

تم إصلاح مشكلة الأزرار. الآن النافذة أكبر والأزرار واضحة وملونة.

## خطوات الاستخدام

### 1. إيقاف التطبيق القديم (إذا كان يعمل)
```powershell
Get-Process | Where-Object {$_.ProcessName -like "*KasserPro*"} | Stop-Process -Force
```

### 2. تشغيل التطبيق الجديد
```powershell
cd G:\POS
Start-Process -FilePath "src\KasserPro.BridgeApp\bin\Debug\net9.0-windows\KasserPro.BridgeApp.exe"
```

أو من File Explorer:
- اذهب إلى: `G:\POS\src\KasserPro.BridgeApp\bin\Debug\net9.0-windows\`
- اضغط double-click على `KasserPro.BridgeApp.exe`

### 3. ابحث عن الأيقونة في System Tray
- انظر بجانب الساعة في شريط المهام
- ستجد أيقونة التطبيق

### 4. افتح نافذة الإعدادات
- اضغط **Double-Click** على الأيقونة
- أو اضغط **Right-Click** → **Settings**

### 5. ستظهر نافذة كبيرة مع:

#### الحقول:
1. **عنوان الخادم - Backend URL**
   - أدخل: `https://localhost:5243`

2. **مفتاح API - API Key**
   - أدخل: `test-api-key-123`
   - (أو أي قيمة للاختبار)

3. **معرف الجهاز - Device ID**
   - (للقراءة فقط - تم إنشاؤه تلقائياً)

4. **الطابعة الافتراضية - Default Printer**
   - اختر طابعة من القائمة
   - أو اضغط "تحديث قائمة الطابعات"
   - للاختبار: اختر "Microsoft Print to PDF"

5. **حالة الاتصال - Connection Status**
   - سيظهر "متصل" أو "غير متصل"

#### الأزرار (في الأسفل):
- **💾 حفظ - Save** (أخضر كبير)
- **❌ إلغاء - Cancel** (أحمر كبير)

### 6. اضغط زر "حفظ"
- ستظهر رسالة: "تم حفظ الإعدادات بنجاح!"
- سيحاول التطبيق الاتصال بالـ Backend

### 7. اختبر الطباعة
- اضغط **Right-Click** على الأيقونة
- اختر **"Test Print"**

## إذا لم تظهر الأزرار

### الحل 1: تكبير النافذة
- اسحب حواف النافذة لتكبيرها
- الأزرار في الأسفل

### الحل 2: Scroll لأسفل
- استخدم scroll bar على يمين النافذة
- انزل للأسفل لرؤية الأزرار

### الحل 3: إعادة تشغيل التطبيق
```powershell
# أوقف التطبيق
Get-Process | Where-Object {$_.ProcessName -like "*KasserPro*"} | Stop-Process -Force

# شغله مرة أخرى
cd G:\POS
Start-Process -FilePath "src\KasserPro.BridgeApp\bin\Debug\net9.0-windows\KasserPro.BridgeApp.exe"
```

## مواصفات النافذة الجديدة

```
العرض: 600 بكسل (قابل للتكبير)
الارتفاع: 550 بكسل (قابل للتكبير)
الحد الأدنى: 500x500 بكسل

الأزرار:
- حجم: 150x45 بكسل
- ألوان واضحة (أخضر وأحمر)
- نصوص عربية وإنجليزية
- أيقونات (💾 و ❌)
```

## تشغيل Backend (مهم!)

قبل الاتصال، شغل الـ Backend:

```bash
cd src/KasserPro.API
dotnet run
```

انتظر حتى ترى:
```
Now listening on: https://localhost:5243
```

## اختبار كامل

### 1. شغل Backend
```bash
cd src/KasserPro.API
dotnet run
```

### 2. شغل Desktop App
```powershell
cd G:\POS
Start-Process -FilePath "src\KasserPro.BridgeApp\bin\Debug\net9.0-windows\KasserPro.BridgeApp.exe"
```

### 3. اضبط Settings
- Double-click على الأيقونة
- أدخل البيانات
- اضغط "حفظ" (الزر الأخضر الكبير)

### 4. اختبر الطباعة
```powershell
Invoke-RestMethod -Uri "https://localhost:5243/api/DeviceTest/test-print" -Method Post -SkipCertificateCheck
```

## عرض Logs

```powershell
Get-Content "$env:APPDATA\KasserPro\logs\bridge-app$(Get-Date -Format 'yyyyMMdd').log" -Tail 20
```

## المشاكل الشائعة

### المشكلة: لا أرى الأزرار
**الحل**: كبر النافذة أو scroll لأسفل

### المشكلة: الأزرار رمادية (disabled)
**الحل**: تأكد من ملء جميع الحقول المطلوبة

### المشكلة: لا يحفظ الإعدادات
**الحل**: 
1. تحقق من صلاحيات الكتابة في `%AppData%`
2. شغل التطبيق كـ Administrator

### المشكلة: النافذة لا تفتح
**الحل**:
1. تحقق من أن التطبيق يعمل في System Tray
2. جرب Right-Click → Settings

## الخلاصة

✅ النافذة الآن أكبر (600x550)
✅ الأزرار واضحة وملونة
✅ نصوص عربية وإنجليزية
✅ قابلة للتكبير
✅ أيقونات واضحة

**جرب الآن!** 🚀
