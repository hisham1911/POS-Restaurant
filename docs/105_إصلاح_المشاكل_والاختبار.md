# 🔧 إصلاح المشاكل والاختبار

**التاريخ**: 9 فبراير 2026

---

## ⚠️ المشكلة

الميزات الجديدة لا تعمل بسبب:
- قاعدة البيانات القديمة لا تحتوي على الحقول الجديدة
- يجب حذف البيانات القديمة وإنشاء بيانات جديدة

---

## ✅ الحل السريع

### طريقة 1: استخدام Script (الأسهل)

```powershell
.\reset-database.ps1
```

هذا سيقوم بـ:
1. حذف قاعدة البيانات القديمة
2. تطبيق جميع Migrations
3. تشغيل Backend (سيُنشئ seed data تلقائياً)

### طريقة 2: يدوياً

```powershell
# 1. حذف قاعدة البيانات
cd src\KasserPro.API
del kasserpro.db
del kasserpro.db-shm
del kasserpro.db-wal

# 2. تطبيق Migrations
dotnet ef database update

# 3. تشغيل Backend
dotnet run
```

---

## 🧪 الاختبار

### 1. تسجيل الدخول
- افتح http://localhost:3000
- Email: `admin@kasserpro.com`
- Password: `Admin@123`

### 2. فتح وردية
1. اذهب إلى "الوردية"
2. انقر "فتح وردية جديدة"
3. أدخل رصيد: `1000`

### 3. اختبار الميزات الجديدة

#### ✅ تسليم الوردية
- انقر زر "تسليم الوردية"
- اختر مستخدم
- أدخل الرصيد
- انقر "تسليم"

#### ✅ قائمة الورديات المفتوحة (Admin)
- scroll لأسفل
- يجب أن ترى قسم "الورديات المفتوحة"

#### ✅ إغلاق بالقوة (Admin)
- في قائمة الورديات
- انقر "إغلاق بالقوة"
- أدخل السبب

#### ✅ صفحة إدارة الورديات
- من القائمة: "إدارة الورديات"
- يجب أن ترى جميع الورديات المفتوحة

#### ✅ استعادة بعد التعطل
- افتح وردية
- انتظر دقيقة
- أغلق المتصفح
- افتح المتصفح مرة أخرى
- يجب أن يظهر modal "استعادة الوردية"

---

## 🐛 إذا لم يعمل

### تحقق من:
1. ✅ حذفت قاعدة البيانات القديمة
2. ✅ طبقت Migrations
3. ✅ Backend يعمل بدون أخطاء
4. ✅ Frontend يعمل بدون أخطاء

### افتح Developer Tools (F12)
1. اذهب إلى Network tab
2. افتح وردية
3. ابحث عن request `/shifts/current`
4. تحقق من response

**يجب أن ترى**:
```json
{
  "lastActivityAt": "...",
  "inactiveHours": 0,
  "isForceClosed": false,
  "isHandedOver": false,
  "durationHours": 0,
  "durationMinutes": 5
}
```

إذا لم ترى هذه الحقول، المشكلة في Backend.

---

## 📋 Checklist

- [ ] حذفت قاعدة البيانات القديمة
- [ ] طبقت Migrations
- [ ] Backend يعمل
- [ ] Frontend يعمل
- [ ] سجلت دخول
- [ ] فتحت وردية
- [ ] الحقول الجديدة موجودة في response
- [ ] زر "تسليم الوردية" يظهر
- [ ] قائمة الورديات المفتوحة تظهر (Admin)
- [ ] صفحة "إدارة الورديات" تعمل

---

## ✅ النتيجة المتوقعة

بعد تطبيق الخطوات:
- ✅ جميع الميزات الجديدة تعمل
- ✅ لا توجد أخطاء في Console
- ✅ لا توجد أخطاء في Backend
- ✅ البيانات منطقية ومتطابقة

---

## 📞 للمساعدة

إذا واجهت مشاكل، أرسل:
1. Screenshot من Network tab
2. Screenshot من Console
3. رسائل الخطأ من Backend
4. رسائل الخطأ من Frontend

---

**ملاحظة**: يجب حذف قاعدة البيانات القديمة. لا يمكن التحديث فقط.

**الوقت المتوقع**: 5-10 دقائق
