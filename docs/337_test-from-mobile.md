# اختبار من الموبايل - Test from Mobile

**التاريخ:** 25 فبراير 2026

---

## 📱 معلومات الشبكة

**IP الجهاز الرئيسي:**
- WiFi: `192.168.1.5`
- Hotspot: `192.168.137.1`

**Backend يستمع على:**
- `0.0.0.0:5243` ✅

---

## 🧪 خطوات الاختبار من الموبايل

### الخطوة 1: تأكد من الشبكة
```
1. افتح إعدادات WiFi على الموبايل
2. تأكد أنك متصل بنفس الشبكة (نفس اسم WiFi)
3. لا تستخدم Mobile Data
```

### الخطوة 2: اختبر الاتصال
```
1. افتح المتصفح على الموبايل
2. اذهب إلى: http://192.168.1.5:5243/api/system/health
3. يجب أن ترى: {"success":true,"status":"healthy"}
```

### الخطوة 3: اختبر تسجيل الدخول
```
1. اذهب إلى: http://192.168.1.5:5243
2. يجب أن ترى صفحة تسجيل الدخول
3. أدخل:
   - Email: admin@kasserpro.com
   - Password: Admin@123
4. اضغط تسجيل الدخول
```

---

## 🔍 الأسباب المحتملة لفشل تسجيل الدخول

### السبب 1: الموبايل على شبكة مختلفة
**الأعراض:**
- الصفحة لا تفتح أصلاً
- "Cannot connect to server"

**الحل:**
```
1. تأكد أن الموبايل والكمبيوتر على نفس WiFi
2. لا تستخدم Guest WiFi
3. لا تستخدم Mobile Data
```

---

### السبب 2: IP تغير
**الأعراض:**
- كان يعمل قبل كده، الآن لا يعمل

**الحل:**
```powershell
# على الكمبيوتر، اعرف IP الجديد
ipconfig | findstr "IPv4"

# استخدم IP الجديد على الموبايل
```

---

### السبب 3: Firewall يحجب
**الأعراض:**
- الصفحة تفتح لكن بطيئة جداً
- أو timeout

**الحل:**
```powershell
# شغل سكريبت الإعداد مرة أخرى
.\setup-network-access.ps1
```

---

### السبب 4: AP Isolation مفعل
**الأعراض:**
- الصفحة لا تفتح من الموبايل
- لكن تعمل من كمبيوتر آخر

**الحل:**
```
1. ادخل إعدادات الراوتر (عادة 192.168.1.1)
2. ابحث عن "AP Isolation" أو "Client Isolation"
3. عطله (Disable)
4. احفظ وأعد تشغيل الراوتر
```

---

### السبب 5: CORS Error
**الأعراض:**
- الصفحة تفتح
- تسجيل الدخول يفشل
- في Console: "CORS policy blocked"

**الحل:**
```
تأكد من أن appsettings.json يحتوي على:
"AllowedOrigins": ["*"]
```

---

### السبب 6: Backend لا يعمل
**الأعراض:**
- "Connection refused"
- "Cannot connect"

**الحل:**
```powershell
# تأكد أن Backend يعمل
netstat -ano | findstr ":5243"
# يجب أن ترى: TCP    0.0.0.0:5243    LISTENING

# إذا لم يكن يعمل، شغله
.\start-backend-network.ps1
```

---

## 🧪 اختبار متقدم

### من الكمبيوتر، اختبر API:
```powershell
# Test health
curl http://192.168.1.5:5243/api/system/health

# Test login
$body = '{"email":"admin@kasserpro.com","password":"Admin@123"}'
Invoke-RestMethod -Uri 'http://192.168.1.5:5243/api/auth/login' -Method POST -Body $body -ContentType 'application/json'
```

---

## 📊 معلومات التشخيص

### جمع معلومات من الكمبيوتر:
```powershell
Write-Host "=== Network Info ===" -ForegroundColor Cyan
ipconfig | Select-String "IPv4"

Write-Host "`n=== Backend Status ===" -ForegroundColor Cyan
netstat -ano | findstr ":5243" | findstr "LISTENING"

Write-Host "`n=== Firewall Rules ===" -ForegroundColor Cyan
Get-NetFirewallRule -DisplayName "*KasserPro*" | Select-Object DisplayName, Enabled

Write-Host "`n=== Recent Logs ===" -ForegroundColor Cyan
$log = Get-ChildItem backend\KasserPro.API\logs\*.log | Sort-Object LastWriteTime -Descending | Select-Object -First 1
Get-Content $log.FullName -Tail 20
```

---

## 💡 نصائح

1. **استخدم IP بدلاً من اسم الجهاز:**
   - ✅ `http://192.168.1.5:5243`
   - ❌ `http://DESKTOP-ABC:5243`

2. **تأكد من الشبكة:**
   - الموبايل والكمبيوتر على نفس WiFi
   - لا تستخدم Guest Network

3. **جرب من متصفح آخر:**
   - Chrome
   - Firefox
   - Safari (iPhone)

4. **امسح Cache:**
   - في المتصفح، امسح cache وcookies
   - أو استخدم Incognito/Private mode

---

## 📞 إذا استمرت المشكلة

أرسل هذه المعلومات:

```
1. IP الكمبيوتر: _______________
2. اسم WiFi: _______________
3. نوع الموبايل: _______________
4. المتصفح: _______________
5. رسالة الخطأ بالضبط: _______________
6. هل الصفحة تفتح؟ نعم / لا
7. هل /api/system/health يعمل؟ نعم / لا
```

---

**تم الإعداد:** 25 فبراير 2026
