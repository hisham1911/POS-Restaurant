# دليل إعداد الاتصال الشبكي - Network Setup Guide

**آخر تحديث:** 25 فبراير 2026  
**الحالة:** ✅ جاهز للاستخدام

---

## 🎯 الهدف

تمكين الأجهزة الأخرى على نفس الشبكة من الوصول إلى KasserPro

---

## 📋 الملفات المتوفرة

| الملف | الوصف | متى تستخدمه |
|------|-------|-------------|
| `setup-network-access.ps1` | إعداد تلقائي للشبكة والـ Firewall | المرة الأولى أو بعد إعادة تثبيت Windows |
| `test-network-access.ps1` | اختبار شامل للإعدادات | للتحقق من أن كل شيء يعمل |
| `NETWORK_ACCESS_FIX.md` | دليل تفصيلي للمشاكل والحلول | عند مواجهة مشكلة |

---

## 🚀 البدء السريع (3 دقائق)

### الخطوة 1: تشغيل سكريبت الإعداد

```powershell
# افتح PowerShell كمسؤول (Run as Administrator)
# انقر بزر الماوس الأيمن على PowerShell → Run as Administrator

cd F:\POS
.\setup-network-access.ps1
```

**ماذا سيحدث:**
- ✅ يعرض عناوين IP الخاصة بك
- ✅ يضيف قاعدة Firewall للسماح بالاتصال
- ✅ يعرض تعليمات الاتصال

---

### الخطوة 2: تشغيل Backend

```powershell
# في نافذة PowerShell جديدة (لا تحتاج صلاحيات مسؤول)
cd F:\POS\backend\KasserPro.API
dotnet run
```

**انتظر حتى ترى:**
```
info: Microsoft.Hosting.Lifetime[14]
      Now listening on: http://0.0.0.0:5243
```

✅ إذا رأيت `0.0.0.0:5243` - ممتاز!  
❌ إذا رأيت `127.0.0.1:5243` - راجع قسم "استكشاف الأخطاء"

---

### الخطوة 3: اختبار الإعدادات

```powershell
# في نافذة PowerShell ثالثة
cd F:\POS
.\test-network-access.ps1
```

**النتيجة المتوقعة:**
```
✅ All tests passed!

Connect from other devices using:
  http://192.168.1.5:5243
```

---

## 🧪 الاختبار اليدوي

### على الجهاز الرئيسي:
1. افتح المتصفح
2. اذهب إلى: `http://localhost:5243`
3. سجل دخول بـ: `admin@kasserpro.com` / `Admin@123`
4. اذهب إلى Settings
5. يجب أن ترى بطاقة "Network Information" مع IP الخاص بك

### على جهاز آخر (نفس الشبكة):
1. اعرف IP الجهاز الرئيسي (مثلاً: `192.168.1.5`)
2. افتح المتصفح على الجهاز الآخر
3. اذهب إلى: `http://192.168.1.5:5243`
4. يجب أن ترى صفحة تسجيل الدخول
5. سجل دخول بنفس البيانات

---

## ✅ معايير النجاح

| الاختبار | النتيجة المتوقعة |
|---------|------------------|
| Backend يعمل | `Now listening on: http://0.0.0.0:5243` |
| Firewall Rule | موجودة ومفعلة |
| Localhost | يفتح بدون مشاكل |
| Network IP | يفتح من جهاز آخر |
| CORS | لا توجد أخطاء في Console |
| Settings Page | تظهر بطاقة Network Info |

---

## 🔧 التغييرات التي تمت

### 1. appsettings.json
```json
{
  "AllowedOrigins": ["*"]
}
```
**السبب:** للسماح لجميع الأجهزة على الشبكة بالاتصال

---

### 2. launchSettings.json
```json
{
  "applicationUrl": "http://0.0.0.0:5243"
}
```
**السبب:** للاستماع على جميع الشبكات وليس localhost فقط

---

### 3. Windows Firewall
```powershell
New-NetFirewallRule -DisplayName "KasserPro API (TCP 5243)" -LocalPort 5243 -Action Allow
```
**السبب:** للسماح بالاتصالات الواردة على المنفذ 5243

---

## 🚨 استكشاف الأخطاء الشائعة

### المشكلة 1: Backend يستمع على 127.0.0.1

**الأعراض:**
```
Now listening on: http://127.0.0.1:5243
```

**الحل:**
```powershell
# 1. أوقف Backend
Get-Process dotnet | Stop-Process -Force

# 2. تحقق من launchSettings.json
# يجب أن يحتوي على: "applicationUrl": "http://0.0.0.0:5243"

# 3. أعد التشغيل
cd backend\KasserPro.API
dotnet run
```

---

### المشكلة 2: CORS Error

**الأعراض:**
```
Access to XMLHttpRequest has been blocked by CORS policy
```

**الحل:**
```json
// في appsettings.json، تأكد من وجود:
{
  "AllowedOrigins": ["*"]
}
```

---

### المشكلة 3: Connection Refused من جهاز آخر

**الأسباب المحتملة:**

1. **Firewall يحجب**
   ```powershell
   # شغل سكريبت الإعداد مرة أخرى
   .\setup-network-access.ps1
   ```

2. **الأجهزة على شبكات مختلفة**
   ```powershell
   # تحقق من IP على كلا الجهازين
   ipconfig | findstr "IPv4"
   # يجب أن يكونا في نفس النطاق (مثلاً: 192.168.1.X)
   ```

3. **AP Isolation مفعل**
   - ادخل إعدادات الراوتر
   - ابحث عن "AP Isolation"
   - عطله (Disable)

4. **Antivirus يحجب**
   - عطل Antivirus مؤقتاً للاختبار
   - إذا نجح، أضف استثناء لـ KasserPro

---

### المشكلة 4: لا يعمل على الموبايل

**الحل:**
1. تأكد أن الموبايل على نفس WiFi
2. استخدم IP بدلاً من اسم الجهاز:
   - ✅ `http://192.168.1.5:5243`
   - ❌ `http://DESKTOP-ABC:5243`
3. لا تستخدم Guest WiFi

---

## 📊 أوامر التشخيص

### معرفة IP
```powershell
ipconfig | findstr "IPv4"
```

### فحص المنفذ
```powershell
netstat -ano | findstr ":5243"
```

### فحص Firewall
```powershell
Get-NetFirewallRule -DisplayName "*KasserPro*"
```

### اختبار API
```powershell
# Localhost
curl http://localhost:5243/api/system/health

# Network IP (استبدل IP)
curl http://192.168.1.5:5243/api/system/health
```

---

## 📚 مراجع إضافية

| الملف | الوصف |
|------|-------|
| `NETWORK_ACCESS_FIX.md` | دليل تفصيلي للإصلاح |
| `ADVANCED_TROUBLESHOOTING_GUIDE.md` | حلول متقدمة |
| `QUICK_SETUP_GUIDE.md` | دليل الإعداد السريع |
| `DOCUMENTATION_INDEX.md` | فهرس جميع التوثيقات |

---

## 🎓 فهم المشكلة

### قبل الإصلاح:
```
Backend يستمع على: 127.0.0.1:5243
                      ↑
                      localhost فقط
                      
الأجهزة الأخرى: ❌ لا تستطيع الوصول
```

### بعد الإصلاح:
```
Backend يستمع على: 0.0.0.0:5243
                      ↑
                      جميع الشبكات
                      
الأجهزة الأخرى: ✅ تستطيع الوصول عبر IP
```

---

## 💡 نصائح

1. **احفظ IP الخاص بك** - سيتغير إذا أعدت تشغيل الراوتر
2. **استخدم Static IP** - في إعدادات الراوتر لتجنب تغيير IP
3. **اختبر بعد كل تحديث Windows** - قد يعطل Firewall rules
4. **احتفظ بنسخة من السكريبتات** - للاستخدام المستقبلي

---

## 📞 الدعم

إذا استمرت المشكلة:

1. شغل `test-network-access.ps1` واحفظ النتائج
2. راجع `NETWORK_ACCESS_FIX.md`
3. راجع `ADVANCED_TROUBLESHOOTING_GUIDE.md`
4. اجمع معلومات التشخيص:
   ```powershell
   ipconfig /all > diagnostics.txt
   netstat -ano | findstr ":5243" >> diagnostics.txt
   Get-NetFirewallRule -DisplayName "*KasserPro*" >> diagnostics.txt
   ```

---

**تم الإعداد بواسطة:** Kiro AI  
**التاريخ:** 25 فبراير 2026  
**الحالة:** ✅ جاهز للاستخدام

---

## 🎯 الخطوات التالية

بعد إتمام الإعداد بنجاح:

1. ✅ اختبر من جهاز آخر
2. ✅ أضف bookmark للـ IP على الأجهزة الأخرى
3. ✅ شارك IP مع المستخدمين الآخرين
4. ✅ راجع Settings → Network Info للتحقق

**استمتع باستخدام KasserPro على جميع أجهزتك! 🎉**
