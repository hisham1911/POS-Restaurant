# ✅ دليل إعادة تعيين قاعدة البيانات - جاهز للتنفيذ

## 📋 الملخص

تم تحديث **DbInitializer.cs** بالكامل مع بيانات جديدة منطقية لمقهى/مطعم:

### ✨ التحديثات المكتملة

#### 1️⃣ الفئات (Categories)
- ☕ مشروبات ساخنة (Hot Drinks)
- 🥤 مشروبات باردة (Cold Drinks)
- 🍔 مأكولات (Food)
- 🍰 حلويات (Desserts)
- 🍿 وجبات خفيفة (Snacks)

#### 2️⃣ المنتجات (Products) - 42 منتج
**مشروبات ساخنة (8):**
- قهوة إسبريسو، كابتشينو، لاتيه، موكا
- شاي أخضر، شاي أسود، شوكولاتة ساخنة، قهوة تركية

**مشروبات باردة (8):**
- عصير برتقال، مانجو، فراولة، سموذي موز
- مياه معدنية، مشروب غازي، آيس كوفي، ليموناضة

**مأكولات (7):**
- برجر لحم، برجر دجاج، ساندويتش كلوب
- بيتزا مارجريتا، باستا ألفريدو، سلطة سيزر، فطيرة جبن

**حلويات (7):**
- كيك شوكولاتة، تشيز كيك، كوكيز، براونيز
- آيس كريم، دونات، كرواسون

**وجبات خفيفة (7):**
- شيبس، فشار، مكسرات، بسكويت
- شوكولاتة، علكة، حلوى

#### 3️⃣ الموردين (Suppliers) - 5 موردين
- شركة البن العربي (قهوة وشاي)
- مؤسسة الألبان الطازجة (منتجات ألبان)
- شركة الفواكه والعصائر (فواكه طازجة)
- مخبز الأمل (مخبوزات وحلويات)
- شركة المواد الغذائية المتحدة (مواد متنوعة)

#### 4️⃣ العملاء (Customers) - 8 عملاء
مع بيانات واقعية: نقاط ولاء، إجمالي طلبات، إجمالي مصروفات

#### 5️⃣ المستخدمين (Users) - 4 مستخدمين
- مدير النظام (Admin)
- أحمد محمد (Cashier)
- فاطمة علي (Cashier)
- محمود حسن (Cashier)

#### 6️⃣ الورديات والطلبات
- 15 وردية (آخر 14 يوم + وردية اليوم مفتوحة)
- طلبات متنوعة مع حالات مختلفة (مكتمل، معلق، ملغي)
- تحديث المخزون تلقائياً بناءً على الطلبات

#### 7️⃣ حقول الورديات الجديدة
- `LastActivityAt` - تم تهيئته لكل وردية
- `IsForceClosed`, `IsHandedOver`, `HandoverBalance` - تم تهيئتهم

---

## 🚀 خطوات إعادة التعيين

### الخطوة 1: إيقاف Backend
```powershell
# إذا كان يعمل في Terminal، اضغط Ctrl+C
# أو ابحث عن العملية وأوقفها:
taskkill /F /IM KasserPro.API.exe
```

### الخطوة 2: حذف قاعدة البيانات القديمة
```powershell
del src\KasserPro.API\kasserpro.db*
```

### الخطوة 3: تطبيق Migrations
```powershell
cd src\KasserPro.API
dotnet ef database update
```

### الخطوة 4: تشغيل Backend
```powershell
dotnet run --project src/KasserPro.API/KasserPro.API.csproj
```

سيتم تشغيل `DbInitializer` تلقائياً وملء قاعدة البيانات بالبيانات الجديدة.

### الخطوة 5: تشغيل Frontend
```powershell
# في terminal آخر
cd client
npm run dev
```

---

## 🧪 اختبار المميزات الجديدة

### 1. تسجيل الدخول
- **Admin:** admin@kasserpro.com / Admin@123
- **Cashier:** ahmed@kasserpro.com / 123456

### 2. اختبار الورديات المحسّنة

#### أ. مراقبة النشاط (Inactivity Monitor)
1. افتح وردية جديدة
2. انتظر 8 ساعات (أو عدّل الوقت في الكود للاختبار)
3. يجب أن تظهر رسالة تحذير

#### ب. تسليم الوردية (Handover)
1. افتح وردية
2. اضغط على زر "تسليم الوردية"
3. اختر مستخدم آخر
4. أدخل الرصيد
5. تأكد من التسليم

#### ج. إغلاق قسري (Force Close) - Admin فقط
1. سجل دخول كـ Admin
2. اذهب إلى "إدارة الورديات"
3. اختر وردية مفتوحة
4. اضغط "إغلاق قسري"
5. أدخل السبب

#### د. استرجاع الوردية (Crash Recovery)
1. افتح وردية
2. أغلق المتصفح فجأة
3. افتح التطبيق مرة أخرى
4. يجب أن تظهر نافذة الاسترجاع

### 3. اختبار المنتجات الجديدة
1. اذهب إلى POS
2. تأكد من ظهور الفئات الجديدة (مشروبات ساخنة، باردة، إلخ)
3. تأكد من ظهور المنتجات الجديدة (قهوة، عصائر، برجر، إلخ)
4. جرب إضافة منتجات للسلة

### 4. اختبار الموردين الجدد
1. اذهب إلى "الموردين"
2. تأكد من ظهور الموردين الجدد (شركة البن العربي، إلخ)

### 5. اختبار فواتير الشراء
1. اذهب إلى "فواتير الشراء"
2. أنشئ فاتورة جديدة
3. اختر مورد (مثل: شركة البن العربي)
4. أضف منتجات (قهوة، شاي)
5. أكمل الفاتورة

---

## 📊 البيانات المتوقعة بعد Reset

| البند | العدد |
|------|------|
| الفئات | 5 |
| المنتجات | 42 |
| الموردين | 5 |
| العملاء | 8 |
| المستخدمين | 4 |
| الفروع | 2 |
| الورديات | 15 (14 مغلقة + 1 مفتوحة) |
| الطلبات | ~150 (متنوعة على 15 يوم) |

---

## ⚠️ ملاحظات مهمة

1. **لا تنسى إيقاف Backend** قبل حذف قاعدة البيانات
2. **كل البيانات القديمة ستُحذف** - لا يمكن التراجع
3. **المخزون سيتم تحديثه تلقائياً** بناءً على الطلبات المكتملة
4. **الوردية المفتوحة** هي وردية اليوم فقط
5. **جميع الحقول الجديدة** تم تهيئتها بشكل صحيح

---

## 🎯 الخطوة التالية

بعد إعادة التعيين، اتبع **FINAL_CHECKLIST_AR.md** لاختبار جميع المميزات بشكل شامل.

---

## 🐛 حل المشاكل

### المشكلة: "database is locked"
**الحل:** أوقف Backend تماماً قبل حذف قاعدة البيانات

### المشكلة: "migration already applied"
**الحل:** احذف قاعدة البيانات أولاً، ثم طبق migrations

### المشكلة: "no data after reset"
**الحل:** تأكد من تشغيل Backend بنجاح - DbInitializer يعمل تلقائياً

---

## ✅ Build Status

```
✅ DbInitializer.cs - Updated
✅ Products - 42 café/restaurant items
✅ Suppliers - 5 food/beverage suppliers
✅ Categories - 5 café categories
✅ Shift fields - LastActivityAt initialized
✅ Build - Successful (0 errors)
```

**جاهز للتنفيذ! 🚀**
