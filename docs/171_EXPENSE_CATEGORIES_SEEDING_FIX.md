# إصلاح مشكلة عدم ظهور تصنيفات المصروفات ✅

## المشكلة

عند محاولة إنشاء مصروف جديد، لا تظهر أي تصنيفات للاختيار منها.

## السبب

لم يتم استدعاء دالة `SeedDefaultCategoriesAsync()` عند بدء التطبيق، مما يعني أن التصنيفات الافتراضية لم يتم إضافتها إلى قاعدة البيانات.

## الحل

تم إضافة استدعاء `SeedDefaultCategoriesAsync()` في `Program.cs` عند تهيئة قاعدة البيانات.

### التصنيفات الافتراضية التي سيتم إضافتها:

1. 💰 **رواتب** (Salaries) - أزرق
2. 🏢 **إيجار** (Rent) - بنفسجي
3. ⚡ **كهرباء** (Electricity) - برتقالي
4. 💧 **مياه** (Water) - سماوي
5. 🔧 **صيانة** (Maintenance) - أخضر
6. 📢 **تسويق** (Marketing) - وردي
7. 🚗 **مواصلات** (Transportation) - نيلي
8. 📞 **اتصالات** (Communications) - تركواز
9. 📝 **مستلزمات مكتبية** (Office Supplies) - برتقالي فاتح
10. 📦 **أخرى** (Other) - رمادي

## التغييرات

### ملف: `src/KasserPro.API/Program.cs`

```csharp
// Seed default expense categories
var expenseCategoryService = scope.ServiceProvider.GetRequiredService<IExpenseCategoryService>();
await expenseCategoryService.SeedDefaultCategoriesAsync();
```

## خطوات التطبيق

1. **أوقف الـ Backend API** (إذا كان يعمل)

2. **أعد بناء المشروع**:
   ```bash
   cd src/KasserPro.API
   dotnet build
   ```

3. **شغّل الـ Backend**:
   ```bash
   dotnet run
   ```

4. **عند بدء التطبيق**:
   - سيتم فحص قاعدة البيانات
   - إذا لم تكن التصنيفات موجودة، سيتم إضافتها تلقائياً
   - إذا كانت موجودة، لن يتم تكرارها

## التحقق من النجاح

1. افتح صفحة **إنشاء مصروف جديد**
2. يجب أن تظهر قائمة منسدلة بـ 10 تصنيفات
3. كل تصنيف يحتوي على:
   - أيقونة (Emoji)
   - اسم بالعربية
   - اسم بالإنجليزية
   - لون مميز

## ملاحظات مهمة

### التصنيفات النظامية (System Categories)
- التصنيفات الافتراضية العشرة هي تصنيفات نظامية (`IsSystem = true`)
- **لا يمكن حذفها** من قبل المستخدمين
- **لا يمكن تعديلها** (ما عدا تفعيلها/تعطيلها)
- هذا يضمن وجود تصنيفات أساسية دائماً

### التصنيفات المخصصة
- يمكن للمستخدمين إضافة تصنيفات جديدة
- يمكن تعديل وحذف التصنيفات المخصصة
- لا يمكن حذف تصنيف يحتوي على مصروفات

### Multi-Tenancy
- كل Tenant يحصل على نسخته الخاصة من التصنيفات
- التصنيفات معزولة بين الـ Tenants
- عند إضافة Tenant جديد، سيحصل على التصنيفات الافتراضية تلقائياً

## الكود المسؤول عن الـ Seeding

```csharp
public async Task SeedDefaultCategoriesAsync()
{
    try
    {
        var tenants = await _unitOfWork.Tenants.Query().ToListAsync();

        foreach (var tenant in tenants)
        {
            // Check if categories already exist
            var exists = await _unitOfWork.ExpenseCategories.Query()
                .AnyAsync(c => c.TenantId == tenant.Id && c.IsSystem);

            if (exists)
                continue;

            var defaultCategories = new List<ExpenseCategory>
            {
                new() { TenantId = tenant.Id, Name = "رواتب", NameEn = "Salaries", Icon = "💰", Color = "#3B82F6", IsActive = true, IsSystem = true, SortOrder = 1 },
                // ... 9 more categories
            };

            foreach (var category in defaultCategories)
            {
                await _unitOfWork.ExpenseCategories.AddAsync(category);
            }

            await _unitOfWork.SaveChangesAsync();
        }
    }
    catch (Exception ex)
    {
        _logger.LogError(ex, "Error seeding default expense categories");
    }
}
```

## الملفات المعدلة

- ✅ `src/KasserPro.API/Program.cs`

---

**الحالة**: ✅ تم الإصلاح - جاهز للاختبار بعد إعادة تشغيل الـ Backend
