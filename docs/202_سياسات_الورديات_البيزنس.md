# 🏪 سياسات الورديات من منظور البيزنس

**السؤال الأساسي:** متى تُقفل الوردية؟ وما علاقتها بالتقرير اليومي؟

---

## 🎯 السيناريوهات الشائعة

### السيناريو 1: وردية واحدة في اليوم (Single Shift Per Day)
```
المحل يفتح: 8 صباحاً
المحل يقفل: 10 مساءً
موظف واحد طول اليوم
```

**السياسة:**
- ✅ وردية واحدة تفتح الساعة 8 صباحاً
- ✅ تقفل الساعة 10 مساءً (أو بعد آخر عميل)
- ✅ الوردية = اليوم الكامل
- ✅ التقرير اليومي = تقرير الوردية

**الفوائد:**
- بسيط وسهل
- مناسب للمحلات الصغيرة
- موظف واحد مسؤول عن اليوم كله

**العيوب:**
- وردية طويلة جداً (14 ساعة!)
- إرهاق الموظف
- صعوبة المساءلة إذا حصل خطأ

---

### السيناريو 2: ورديات متعددة في اليوم (Multiple Shifts Per Day) ⭐ الأفضل
```
الوردية الصباحية: 8 ص - 4 م (موظف أ)
الوردية المسائية: 4 م - 10 م (موظف ب)
```

**السياسة:**
- ✅ كل موظف يفتح ويقفل ورديته
- ✅ الموظف أ يقفل الساعة 4 م
- ✅ الموظف ب يفتح الساعة 4 م
- ✅ التقرير اليومي = مجموع الورديتين

**الفوائد:**
- ✅ ورديات معقولة (8 ساعات)
- ✅ مساءلة أفضل (كل موظف مسؤول عن ورديته)
- ✅ راحة للموظفين
- ✅ سهولة تحديد مصدر الأخطاء

**العيوب:**
- يحتاج تنسيق بين الموظفين
- عملية تسليم واستلام

---

### السيناريو 3: ورديات متداخلة (Overlapping Shifts)
```
الوردية 1: 8 ص - 4 م (موظف أ)
الوردية 2: 12 ظ - 8 م (موظف ب)
الوردية 3: 4 م - 10 م (موظف ج)
```

**السياسة:**
- ✅ كل موظف له درج نقدي خاص
- ✅ كل موظف يقفل ورديته عند انتهاء دوامه
- ✅ التقرير اليومي = مجموع جميع الورديات

**الفوائد:**
- مرونة عالية
- تغطية أوقات الذروة
- مساءلة فردية

**العيوب:**
- معقد في الإدارة
- يحتاج نظام POS متقدم
- يحتاج مساحة لعدة أدراج نقدية

---

## 📅 متى تُقفل الوردية؟

### القاعدة الذهبية:
```
الوردية تُقفل عند انتهاء دوام الموظف، وليس عند إغلاق المحل
```

### الأمثلة:

#### مثال 1: محل صغير
```
المحل: 8 ص - 10 م
الموظف: واحد فقط

الحل:
- وردية واحدة من 8 ص إلى 10 م
- تُقفل عند إغلاق المحل
```

#### مثال 2: محل متوسط
```
المحل: 8 ص - 10 م
الموظفين: اثنين (صباحي ومسائي)

الحل:
- وردية صباحية: 8 ص - 4 م
- وردية مسائية: 4 م - 10 م
- كل وردية تُقفل عند انتهاء دوام الموظف
```

#### مثال 3: محل كبير
```
المحل: 8 ص - 12 م
الموظفين: 3-4 موظفين في نفس الوقت

الحل:
- كل موظف له وردية خاصة
- كل وردية تُقفل عند انتهاء دوام الموظف
- قد يكون هناك 5-6 ورديات في اليوم الواحد
```

---

## 🔄 علاقة الوردية بالتقرير اليومي

### المفهوم الأساسي:
```
التقرير اليومي = مجموع جميع الورديات في ذلك اليوم
```

### مثال عملي:

```
اليوم: الأحد 10 فبراير 2026

الوردية 1 (أحمد - 8 ص إلى 4 م):
- المبيعات: 5,000 جنيه
- عدد الطلبات: 45
- نقدي: 3,500 جنيه
- بطاقة: 1,500 جنيه

الوردية 2 (محمد - 4 م إلى 10 م):
- المبيعات: 7,000 جنيه
- عدد الطلبات: 60
- نقدي: 4,500 جنيه
- بطاقة: 2,500 جنيه

التقرير اليومي (الأحد):
- إجمالي المبيعات: 12,000 جنيه
- إجمالي الطلبات: 105
- إجمالي النقدي: 8,000 جنيه
- إجمالي البطاقات: 4,000 جنيه
```

---

## ⚠️ ماذا لو نسي الكاشير إقفال الوردية؟

### المشكلة:
```
الموظف انتهى دوامه الساعة 4 م
نسي يقفل الوردية
راح البيت
الوردية لسه مفتوحة!
```

### الحلول المتاحة:

#### الحل 1: الإغلاق القسري (Force Close) ⭐ الأفضل
```
المدير يقفل الوردية قسرياً:
- يسجل سبب الإغلاق القسري
- يعد النقود
- يسجل الفرق
- النظام يوثق كل شيء
```

**متى يُستخدم:**
- الموظف نسي ومشي
- الموظف مريض فجأة
- طوارئ

**نظامك يدعمه:** ✅ نعم! (IsForceClosed, ForceCloseReason)

---

#### الحل 2: الإغلاق التلقائي (Auto Close)
```
النظام يقفل الوردية تلقائياً:
- في وقت محدد (مثلاً 11 م)
- إذا لم يتم إقفالها يدوياً
- يسجل أنها أُقفلت تلقائياً
```

**متى يُستخدم:**
- سياسة ثابتة للمحل
- منع الورديات المفتوحة طول الليل
- الأتمتة

**نظامك يحتاج:** ⚠️ إضافة هذه الميزة

**الكود المقترح:**
```csharp
public class AutoCloseShiftService
{
    // يعمل كل ساعة
    public async Task AutoCloseOpenShifts()
    {
        var now = DateTime.UtcNow;
        var autoCloseTime = new TimeSpan(23, 0, 0); // 11 مساءً
        
        // ابحث عن الورديات المفتوحة أكثر من 12 ساعة
        var openShifts = await _context.Shifts
            .Where(s => !s.IsClosed && 
                   (now - s.OpenedAt).TotalHours > 12)
            .ToListAsync();
        
        foreach (var shift in openShifts)
        {
            // أقفل تلقائياً
            shift.IsClosed = true;
            shift.ClosedAt = now;
            shift.IsForceClosed = true;
            shift.ForceCloseReason = "إغلاق تلقائي - تجاوزت 12 ساعة";
            shift.ForceClosedByUserName = "النظام";
            shift.ForceClosedAt = now;
            
            // احسب الرصيد المتوقع
            shift.ExpectedBalance = CalculateExpectedBalance(shift);
            shift.ClosingBalance = shift.ExpectedBalance; // افتراضي
            shift.Difference = 0; // لم يتم العد الفعلي
            
            // سجل ملاحظة
            shift.Notes = "تم الإغلاق تلقائياً - يحتاج مراجعة يدوية";
        }
        
        await _context.SaveChangesAsync();
    }
}
```

---

#### الحل 3: تنبيه المدير
```
النظام يرسل تنبيه للمدير:
- الوردية مفتوحة أكثر من X ساعات
- المدير يتخذ الإجراء المناسب
```

**متى يُستخدم:**
- المدير موجود
- يحتاج مراجعة يدوية

---

## 📊 أفضل الممارسات العالمية

### من Microsoft Dynamics 365:
```
"Typically, shifts are opened at the start of the business day. 
At that point, a user declares the starting amount. 
Sales transactions are then performed throughout the day. 
Finally, at the end of the day, the drawer is counted, 
and the closing amounts are declared."
```

**الترجمة:**
- الوردية تفتح في بداية يوم العمل
- تُقفل في نهاية اليوم
- **ملاحظة:** هذا للمحلات الصغيرة بموظف واحد

---

### من Cloud Retailer:
```
"Most users end the shift once per POS station at the end of the day, 
some execute this process at the end of each shift 
(may be more than one shift per day)."
```

**الترجمة:**
- معظم المستخدمين: وردية واحدة في اليوم
- البعض: ورديات متعددة حسب دوام الموظفين
- **الخيار متروك لك!**

---

### من NCR Silver:
```
"If no open orders or shifts exist, 
the system automatically closes the business date. 
If open orders or shifts exist, 
the system keeps attempting to close the business date."
```

**الترجمة:**
- النظام يحاول إقفال اليوم تلقائياً
- إذا كان هناك ورديات مفتوحة، ينتظر
- **الإغلاق التلقائي ذكي**

---

## 🎯 التوصيات لنظامك

### 1. السياسة الموصى بها: ورديات متعددة ⭐

```
لماذا؟
✅ ورديات معقولة (6-8 ساعات)
✅ مساءلة أفضل
✅ راحة للموظفين
✅ سهولة تحديد الأخطاء
```

**التطبيق:**
```
محل يعمل 14 ساعة (8 ص - 10 م):

الوردية 1: 8 ص - 4 م (8 ساعات)
الوردية 2: 4 م - 10 م (6 ساعات)

كل موظف:
- يفتح ورديته عند بداية دوامه
- يقفل ورديته عند نهاية دوامه
- مسؤول عن درجه النقدي فقط
```

---

### 2. إضافة الإغلاق التلقائي ⭐⭐

```csharp
// في appsettings.json
{
  "ShiftSettings": {
    "AutoCloseEnabled": true,
    "AutoCloseAfterHours": 12,
    "AutoCloseTime": "23:00",
    "SendAlertBeforeAutoClose": true,
    "AlertHoursBeforeClose": 1
  }
}
```

**الفوائد:**
- منع الورديات المفتوحة طول الليل
- حماية من النسيان
- تقارير يومية دقيقة

---

### 3. تنبيهات الوردية المفتوحة ⭐⭐⭐

```
التنبيه 1: بعد 8 ساعات
"تنبيه: الوردية مفتوحة منذ 8 ساعات"

التنبيه 2: بعد 10 ساعات
"تحذير: الوردية مفتوحة منذ 10 ساعات - يُنصح بالإقفال"

التنبيه 3: بعد 12 ساعة
"تحذير شديد: الوردية مفتوحة منذ 12 ساعة - سيتم الإقفال التلقائي قريباً"
```

---

### 4. التقرير اليومي الموحد ⭐⭐⭐

```
التقرير اليومي يجب أن يحتوي على:

1. ملخص اليوم:
   - إجمالي المبيعات
   - إجمالي الطلبات
   - إجمالي النقدي/البطاقات

2. تفصيل الورديات:
   - الوردية 1 (أحمد): 5,000 جنيه
   - الوردية 2 (محمد): 7,000 جنيه
   - الإجمالي: 12,000 جنيه

3. الفروقات:
   - الوردية 1: -5 جنيه (مقبول)
   - الوردية 2: +10 جنيه (مقبول)
   - الإجمالي: +5 جنيه

4. الإحصائيات:
   - متوسط قيمة الطلب
   - أكثر المنتجات مبيعاً
   - أوقات الذروة
```

---

## 📋 سياسة مقترحة لمحلك

### السياسة العامة:

```
1. الورديات:
   ✅ كل موظف يفتح وردية عند بداية دوامه
   ✅ كل موظف يقفل وردية عند نهاية دوامه
   ✅ الحد الأقصى للوردية: 8 ساعات
   ✅ لا يُسمح بمشاركة الدرج النقدي

2. الإقفال:
   ✅ الموظف يقفل ورديته قبل المغادرة
   ✅ إذا نسي، المدير يقفلها قسرياً
   ✅ النظام يقفل تلقائياً بعد 12 ساعة

3. التقرير اليومي:
   ✅ يُطبع في نهاية اليوم (بعد آخر وردية)
   ✅ يحتوي على جميع الورديات
   ✅ يُراجع من المدير صباح اليوم التالي

4. الفروقات:
   ✅ ±5 جنيه: مقبول
   ✅ ±6-20 جنيه: تحذير شفهي
   ✅ ±21-50 جنيه: تحذير كتابي
   ✅ أكثر من 50: تحقيق

5. الطوارئ:
   ✅ المدير يمكنه إقفال أي وردية قسرياً
   ✅ يجب تسجيل السبب
   ✅ يُراجع في اليوم التالي
```

---

## 🔄 سيناريوهات عملية

### السيناريو 1: يوم عادي
```
8:00 ص - أحمد يفتح وردية
4:00 م - أحمد يقفل ورديته (5,000 جنيه)
4:00 م - محمد يفتح وردية
10:00 م - محمد يقفل ورديته (7,000 جنيه)
10:30 م - المدير يطبع التقرير اليومي (12,000 جنيه)
```

### السيناريو 2: موظف نسي
```
8:00 ص - أحمد يفتح وردية
4:00 م - أحمد نسي يقفل ومشي!
4:05 م - محمد يحاول يفتح وردية → خطأ (وردية أحمد لسه مفتوحة)
4:10 م - المدير يقفل وردية أحمد قسرياً
4:15 م - محمد يفتح ورديته
10:00 م - محمد يقفل ورديته
```

### السيناريو 3: طوارئ
```
8:00 ص - أحمد يفتح وردية
12:00 ظ - أحمد مرض فجأة ومشي المستشفى!
12:30 م - المدير يقفل وردية أحمد قسرياً (السبب: طوارئ صحية)
1:00 م - محمد يفتح وردية بديلة
10:00 م - محمد يقفل ورديته
```

---

## ✅ Checklist التطبيق

### في النظام:
- [x] فتح وردية ✅ موجود
- [x] إقفال وردية ✅ موجود
- [x] إقفال قسري ✅ موجود
- [ ] إقفال تلقائي ⚠️ يحتاج إضافة
- [ ] تنبيهات الوردية الطويلة ⚠️ يحتاج إضافة
- [x] التقرير اليومي ✅ موجود (يحتاج تحسين)

### في السياسات:
- [ ] كتابة سياسة الورديات
- [ ] تدريب الموظفين
- [ ] توضيح متى تُقفل الوردية
- [ ] توضيح ماذا يحدث إذا نسي الموظف

---

## 🎉 الخلاصة

### الإجابة على أسئلتك:

**1. متى تُقفل الوردية؟**
```
✅ عند انتهاء دوام الموظف (وليس إغلاق المحل)
✅ يمكن أن يكون هناك عدة ورديات في اليوم
✅ الأفضل: ورديات 6-8 ساعات
```

**2. لو الكاشير نسي؟**
```
✅ المدير يقفلها قسرياً (نظامك يدعمه!)
✅ أو النظام يقفلها تلقائياً (يحتاج إضافة)
✅ تُسجل كـ "إقفال قسري" مع السبب
```

**3. علاقتها بالتقرير اليومي؟**
```
✅ التقرير اليومي = مجموع جميع الورديات
✅ كل وردية لها تقريرها الخاص
✅ التقرير اليومي يُطبع في نهاية اليوم
```

---

**نظامك جاهز تقنياً! فقط يحتاج:**
1. ✅ إضافة الإقفال التلقائي
2. ✅ إضافة التنبيهات
3. ✅ كتابة السياسات
4. ✅ تدريب الموظفين

**المراجع:**
- Microsoft Dynamics 365 - Shift Management
- NCR Silver - Business Date Closing
- Cloud Retailer - End of Day Process
- Eatos - Automating End of Day Tasks
