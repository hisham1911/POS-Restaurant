# 🛡️ دليل استثناءات Windows Defender

## المشكلة
Windows Defender بيعمل فحص مستمر للملفات أثناء البناء والتطوير، وده بيسبب:
- بطء شديد في عملية البناء (Build)
- بطء في تشغيل `dotnet run` و `npm start`
- استهلاك عالي للـ CPU والـ Disk

## الحل
إضافة استثناءات لمجلد المشروع والعمليات المستخدمة.

---

## 🚀 طريقة الاستخدام

### 1️⃣ إضافة الاستثناءات (مرة واحدة)

```powershell
# افتح PowerShell كـ Administrator
# انتقل لمجلد المشروع
cd "D:\مسح\POS"

# شغل السكريبت
.\add-defender-exclusion.ps1
```

### 2️⃣ التحقق من الاستثناءات

```powershell
# عرض جميع الاستثناءات
Get-MpPreference | Select-Object -ExpandProperty ExclusionPath
Get-MpPreference | Select-Object -ExpandProperty ExclusionProcess
```

### 3️⃣ إزالة الاستثناءات (إذا احتجت)

```powershell
.\remove-defender-exclusion.ps1
```

---

## 📋 الاستثناءات المضافة

### المجلدات
- `D:\مسح\POS` (المجلد الرئيسي)
- `backend\` (كل ملفات الـ Backend)
- `backend\KasserPro.API\bin\` (ملفات البناء)
- `backend\KasserPro.API\obj\` (ملفات مؤقتة)
- `client\node_modules\` (مكتبات Node.js)
- `client\.next\` (ملفات Next.js المبنية)

### العمليات (Processes)
- `dotnet.exe` (.NET Runtime)
- `node.exe` (Node.js)
- `npm.exe` (NPM)
- `yarn.exe` (Yarn)

---

## ⚡ النتائج المتوقعة

| العملية | قبل | بعد | التحسن |
|---------|-----|-----|--------|
| `dotnet build` | ~30-60 ثانية | ~5-10 ثواني | 🚀 6x أسرع |
| `dotnet run` | ~15-20 ثانية | ~3-5 ثواني | 🚀 4x أسرع |
| `npm install` | ~5-10 دقائق | ~1-2 دقيقة | 🚀 5x أسرع |
| `npm run dev` | ~10-15 ثانية | ~2-3 ثواني | 🚀 5x أسرع |

---

## 🔒 ملاحظات أمنية

### ✅ آمن لأن:
1. الاستثناء فقط لمجلد المشروع (ليس للنظام كله)
2. الكود الخاص بك تحت سيطرتك
3. لا يؤثر على حماية باقي النظام

### ⚠️ احذر من:
- تحميل مكتبات من مصادر غير موثوقة
- نسخ كود من مصادر مشبوهة
- تشغيل ملفات `.exe` غير معروفة

---

## 🛠️ استكشاف الأخطاء

### خطأ: "Access Denied"
**الحل:** شغل PowerShell كـ Administrator

### خطأ: "Exclusion already exists"
**الحل:** الاستثناء موجود بالفعل، كل شيء تمام ✅

### السكريبت لا يعمل
**الحل:**
```powershell
# تفعيل تشغيل السكريبتات (مرة واحدة)
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

---

## 📞 دعم إضافي

إذا واجهت أي مشكلة:
1. تأكد من تشغيل PowerShell كـ Administrator
2. تأكد من المسار الصحيح للمشروع
3. جرب إعادة تشغيل الجهاز بعد إضافة الاستثناءات

---

## 🔄 إلغاء التفعيل

لإلغاء جميع الاستثناءات:
```powershell
.\remove-defender-exclusion.ps1
```

أو يدوياً من:
```
Windows Security → Virus & threat protection → 
Manage settings → Exclusions → Remove
```
