# 🎨 KasserPro Design System (v2.0)
## نظام التصميم وتجربة المستخدم (User-Centered POS Design)

وثيقة مرجعية شاملة لتطوير واجهة نقطة بيع (POS) تركز على **سرعة الموظف**، **سهولة القراءة**، و **كفاءة التشغيل**.

---

# 📑 الفهرس

1. [فلسفة التصميم وتجربة المستخدم](#-1-فلسفة-التصميم-تجربة-الموظف-cashier-ux)
2. [الأسس البصرية (Visual Foundation)](#-2-الأسس-البصرية-visual-foundation)
3. [نظام المكونات (Component System)](#-3-نظام-المكونات-component-system)
4. [هيكلية الشاشات (Layouts)](#-4-هيكلية-الشاشات-layouts)
5. [التفاعل والاختصارات (Interaction & Shortcuts)](#-5-التفاعل-والاختصارات-interaction--shortcuts)
6. [إرشادات التطوير (Dev Guidelines)](#-6-إرشادات-التطوير-dev-guidelines)

---

# 🧠 1. فلسفة التصميم: تجربة الموظف (Cashier UX)

الهدف الأول ليس الجمال فقط، بل **الكفاءة**. الموظف يستخدم النظام لمئات العمليات يومياً.

### المبادئ الثلاثة (The 3 Pillars)
1.  **السرعة (Speed):** تقليل عدد النقرات. كل عملية يجب أن تتم في أقل عدد ممكن من الخطوات (مثلاً: إضافة منتج = نقرة واحدة).
2.  **الوضوح (Glanceability):** يمكن تمييز المعلومات (السعر، المنتج، الإجمالي) بلمحة بصر سريعة وسط زحمة العمل.
3.  **التسامح (Forgiveness):** سهولة تصحيح الأخطاء (حذف منتج، تعديل كمية) دون الحاجة لإعادة العملية.

### قواعد اللمس (Touch-First Design)
*   **Target Size:** أصغر عنصر قابل للنقر يجب أن لا يقل عن **44px × 44px**.
*   **Safe Zones:** الأزرار الخطرة (مثل "إلغاء الطلب") يجب أن تكون بعيدة عن أزرار الاستخدام المتكرر (مثل "الدفع").

---

# 🎨 2. الأسس البصرية (Visual Foundation)

## الألوان (Colors)
نستخدم ألواناً وظيفية لتقليل الجهد الذهني.

```scss
// Primary - للعمليات الرئيسية
$primary-600: #2563eb; // زر الدفع، الفئات النشطة
$primary-50:  #eff6ff; // خلفية العناصر المحددة

// Semantic Colors - ذات معنى وظيفي
$success-600: #059669; // تأكيد، إتمام، دفع نقدي
$danger-600:  #dc2626; // حذف، إلغاء، خصم
$warning-500: #f59e0b; // ملاحظات، تنبيهات
$gray-900:    #111827; // العناوين، الأسعار النهائية
$gray-500:    #6b7280; // النصوص الثانوية
```

## التباين والخطوط (Typography & Contrast)
*   **الخط:** `Cairo` (يدعم العربية بوضوح ممتاز).
*   **الأرقام:** `Inter` (لأن الأرقام هي أهم بيانات في الـ POS).
*   **الأحجام:**
    *   **أسماء المنتجات:** `font-medium text-base` (سهلة القراءة).
    *   **الأسعار (Grid):** `font-bold text-lg text-primary-600` (لجذب العين).
    *   **الإجمالي النهائي:** `font-extrabold text-4xl` (واضح جداً للعميل والموظف).

---

# 🧱 3. نظام المكونات (Component System)

## أ. بطاقة المنتج (Product Card)
مصممة للسرعة البصرية.
*   **الحاوية:** حدود ناعمة `rounded-xl`، ظل خفيف `shadow-sm` يزداد عند التحويم `hover:shadow-md`.
*   **الصورة:** تحتل 60% من مساحة الكارت. في حال عدم وجود صورة، نعرض أيقونة التصنيف أو لون مميز.
*   **التفاعل:** تأثير `active:scale-95` لإعطاء شعور ملموس بالضغط (Tactile Feedback).

## ب. سلة المشتريات (The Cart)
*   **موقعها:** دائماً في الجانب (يمين أو يسار حسب اللغة)، ثابتة لا تتحرك.
*   **العناصر:**
    *   زر **[+]** و **[-]** كبيران وسهلان اللمس.
    *   زر **حذف** (سلة مهملات) بلون أحمر خافت يظهر بوضوح عند الحاجة.

## ج. أزرار الدفع (Action Buttons)
*   **زر "الدفع" (Pay):** يأخذ عرض السلة بالكامل، بلون أخضر أو أزرق مميز، ويحتوي على المبلغ الإجمالي.
*   **أزرار الحاسبة (Numpad):** في مودال الدفع، الأزرار كبيرة جداً لسهولة الإدخال السريع.

---

# 📺 4. هيكلية الشاشات (Layouts)

## شاشة نقطة البيع (POS Main Screen)
تقسيم الشاشة بنسبة **70% للمنتجات** و **30% للسلة**.

```
┌───────────────────────────┬──────────────┐
│  Categories Bar (Tabs)    │  Header      │
│  [☕] [🥐] [🥤] [🍟]      │  (User Info) │
├───────────────────────────┼──────────────┤
│                           │              │
│  Grid of Products         │  Cart List   │
│  (Scrollable)             │  (Scrollable)│
│                           │              │
│  ┌──┐ ┌──┐ ┌──┐ ┌──┐      │  1. Coffee   │
│  │📦│ │📦│ │📦│ │📦│      │  2. Cake     │
│  └──┘ └──┘ └──┘ └──┘      │              │
│                           │              │
│                           │  Total Panel │
│                           │  [ PAY $$$ ] │
└───────────────────────────┴──────────────┘
```

## إدارة المساحات (Spacing Rules)
*   الفراغات بين المنتجات: `gap-3` (حوالي 12px) لتسمح بوضع أكبر عدد دون تلاصق مزعج.
*   Padding الحاويات: `p-4` (مريح للعين).

---

# ⚡ 5. التفاعل والاختصارات (Interaction & Shortcuts)

لجعل الموظف "Power User":

### اختصارات الكيبورد (Keyboard Hotkeys)
*   `F12` : إتمام الدفع / فتح نافذة الدفع.
*   `Esc` : إغلاق النوافذ / إلغاء التحديد.
*   `F2`  : البحث عن منتج.
*   `Delete`: حذف العنصر المحدد من السلة.

### التغذية الراجعة (Feedback)
*   **نجاح:** صوت خفيف "Beep" أو ومضة خضراء عند إضافة منتج.
*   **خطأ:** اهتزاز الحقل (Shake animation) أو توست أحمر عند نفاد الكمية.

---

# 🛠️ 6. إرشادات التطوير (Dev Guidelines)

### قواعد RTL / Layout
*   استخدم `min-w-0` في حاويات الـ Flex لمنع المشاكل.
*   استخدم `grid-cols-custom` تستجيب لحجم الشاشة (من 2 أعمدة في الموبايل إلى 6 في الشاشات العريضة).

### معالجة الصور (Image Handling)
*   دائماً استخدم Fallback كما في كود `ProductCard`.
*   إذا فشلت الصورة، اعرض أيقونة التصنيف.

### الأداء (Performance)
*   استخدم `React.memo` لبطاقات المنتجات لتجنب إعادة الرسم غير الضروري عند تحديث السلة.
*   استخدم `Virtualization` لقائمة المنتجات إذا تجاوز العدد 500 منتج.

---
**الخلاصة:** هذا النظام مصمم ليخدم "الموظف المشغول". كل بكسل، وكل لون، وكل مسافة وضعت لتجعل عملية البيع أسرع وأسهل.
